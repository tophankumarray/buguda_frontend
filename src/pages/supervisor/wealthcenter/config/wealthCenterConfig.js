export const MCC_CUBES = Array.from({ length: 14 }, (_, i) => `${i + 1}`);
export const MRF_CUBES = Array.from({ length: 6 }, (_, i) => `${i + 1}`);

export const STATUS_OPTIONS = ["All", "Stored", "Sent", "Received", "Sold"];

export const AGENCY_MATERIALS = ["Plastic", "Paper", "Metal", "Glass"];
